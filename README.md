# RPS-AI
This project simulates a rock, paper, scissors game against an AI that detects patterns in your input and chooses an optimal move based on the probability of your next input. Changing the window size allows for more complex pattern learning (Higher window) and faster pattern learning (Lower window).
